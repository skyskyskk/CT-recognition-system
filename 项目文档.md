# 远程医疗诊断系统 - 项目文档

## 1. 项目概述

**项目名称**: Telemedicine (南京市鼓楼医院远程诊断系统)

**技术栈**: Qt 5 + C++ + OpenCV + MySQL

**功能描述**: 基于 Qt 框架开发的远程医疗诊断系统，支持患者信息管理、CT 影像读取与处理（霍夫圆检测）、病历查看等功能。

---

## 2. 项目结构

```
Telemedicine/
├── main.cpp              # 程序入口，数据库连接
├── mainwindow.h          # 主窗口类声明
├── mainwindow.cpp        # 主窗口类实现
├── mainwindow.ui         # Qt Designer UI 文件
├── Telemedicine.pro      # Qt 项目配置文件
├── packages/             # 第三方库
│   ├── opencv4_x64-windows/   # OpenCV 库
│   └── mysql_lib/x64_8.1/     # MySQL 客户端库
└── dll/                  # 运行时 DLL 文件
```

---

## 3. 类图

```mermaid
classDiagram
    class QApplication {
        +int exec()
    }

    class QMainWindow {
        +show()
    }

    class MainWindow {
        -Ui::MainWindow *ui
        -Mat myCtImg
        -Mat myCtGrayImg
        -QImage myCtQImage
        -QSqlTableModel *model
        -QSqlTableModel *model_d
        -QTimer *myTimer
        +MainWindow(QWidget *parent)
        +~MainWindow()
        -initMainWindow()
        -ctImgShow()
        -ctImgRead()
        -ctImgProc()
        -ctImgSave()
        -ctImgHoughCircles()
        -showUserPhoto()
        -onTableSelectChange(int row)
        -onTimeOut()
        -on_startPushButton_clicked()
        -on_basicTableView_clicked(QModelIndex)
        -on_tabWidget_tabBarClicked(int index)
    }

    class QSqlTableModel {
        +setTable(QString)
        +select()
        +data(QModelIndex)
        +rowCount()
    }

    class QTimer {
        +start()
        +setInterval(int)
    }

    class Mat_OpenCV {
        +cols
        +rows
        +data
    }

    class QImage {
        +save(QIODevice, format)
    }

    class createMySqlConn {
        <<static function>>
        +bool createMySqlConn()
    }

    QMainWindow <|-- MainWindow
    MainWindow --> QSqlTableModel : model (basic_inf)
    MainWindow --> QSqlTableModel : model_d (details_inf)
    MainWindow --> QTimer : myTimer
    MainWindow --> Mat_OpenCV : myCtImg, myCtGrayImg
    MainWindow --> QImage : myCtQImage
    QApplication --> MainWindow : creates
    main.cpp --> createMySqlConn : calls
    createMySqlConn ..> QSqlDatabase : uses
```

---

## 4. 流程图

### 4.1 程序启动流程

```mermaid
flowchart TD
    A[程序启动 main] --> B{检查 QMYSQL 驱动}
    B -->|可用| C[调用 createMySqlConn]
    B -->|不可用| E[提示驱动错误]
    C --> D{连接 MySQL 成功?}
    D -->|是| F[创建 MainWindow]
    D -->|否| G[尝试启动 MySQL80 服务]
    G --> H[等待服务启动]
    H --> I[等待 3 秒]
    I --> J{再次连接数据库}
    J -->|成功| F
    J -->|失败| K[弹出错误对话框]
    K --> L[退出程序]
    F --> M[显示主窗口]
    M --> N[进入事件循环 a.exec]
```

### 4.2 主窗口初始化流程

```mermaid
flowchart TD
    A[MainWindow 构造函数] --> B[ui->setupUi]
    B --> C[initMainWindow]
    C --> D[加载默认 CT 图像 Tumor.jpg]
    D --> E[BGR 转 RGB]
    E --> F[生成 QImage 并显示]
    C --> G[设置日期 LCD 显示]
    C --> H[创建 QTimer 1秒间隔]
    H --> I[连接 timeout 信号到 onTimeOut]
    B --> J[创建 QSqlTableModel model]
    J --> K[绑定 basic_inf 表]
    K --> L[创建 QSqlTableModel model_d]
    L --> M[绑定 details_inf 表]
    M --> N[basicTableView 设置 model]
    N --> O[onTableSelectChange 0 初始化显示]
```

### 4.3 诊断流程（点击"开始诊断"）

```mermaid
flowchart TD
    A[用户点击 开始诊断] --> B[ctImgRead: 弹出文件选择对话框]
    B --> C{用户选择图像?}
    C -->|否| Z[返回]
    C -->|是| D[OpenCV imread 读取图像]
    D --> E[BGR 转 RGB, 转灰度]
    E --> F[更新显示 CT 图像]
    F --> G[进度条动画 5秒等待]
    G --> H[ctImgProc: 图像处理]
    H --> I[ctImgHoughCircles: 霍夫圆检测]
    I --> J[高斯模糊 9x9]
    J --> K[HoughCircles 检测圆]
    K --> L[在图像上绘制检测到的圆]
    L --> M[更新进度条]
    M --> N[显示处理后的图像]
    N --> O[弹出诊断结果: 疑似子宫肌瘤]
    O --> P[ctImgSave: 保存为 Tumor_proced.jpg]
    P --> Q[进度条归零]
```

### 4.4 患者信息切换流程

```mermaid
flowchart TD
    A[用户点击 basicTableView 某行] --> B[on_basicTableView_clicked]
    B --> C[onTableSelectChange]
    C --> D[从 model 读取姓名]
    D --> E[设置 nameLabel]
    C --> F[从 model 读取性别]
    F --> G[设置 male/female RadioButton]
    C --> H[从 model 读取出生日期]
    H --> I[计算年龄并设置 ageSpinBox]
    C --> J[从 model 读取民族]
    J --> K[设置 ethniComboBox]
    C --> L[从 model 读取社保号]
    L --> M[设置 ssnLineEdit]
    C --> N[showUserPhoto]
    N --> O[遍历 model_d 查找匹配姓名]
    O --> P[读取 Base64 编码照片]
    P --> Q[解码并显示到 photoLabel]
```

---

## 5. 数据库表结构

| 表名        | 说明         | 主要字段                        |
| ----------- | ------------ | ------------------------------- |
| basic_inf   | 患者基本信息 | SSN, 姓名, 性别, 民族, 出生日期 |
| details_inf | 患者详细信息 | 姓名, 病历描述, 照片(Base64)    |

---

## 6. 关键依赖

| 依赖                | 用途                   |
| ------------------- | ---------------------- |
| Qt 5 (Widgets, Sql) | GUI 框架与数据库访问   |
| OpenCV 4            | 图像处理（霍夫圆检测） |
| MySQL 8.0           | 患者数据存储           |
| QMYSQL 驱动         | Qt MySQL 数据库驱动    |

---

## 7. 编译与运行

**编译命令** (Release 模式):

```bash
qmake Telemedicine.pro -spec win32-g++ "CONFIG+=release"
mingw32-make release
```

**运行前准备**:

1. 确保 MySQL80 服务已启动
2. 数据库 `patient` 已创建，包含 `basic_inf` 和 `details_inf` 表
3. 将 `qsqlmysql.dll` 放置在 `plugins/sqldrivers/` 目录下
4. 将 OpenCV 和 MySQL 相关 DLL 放置在可执行文件同目录或系统 PATH 中

**数据库连接信息**:

- Host: 127.0.0.1:3306
- Database: patient
- User: root
- Password: 123456
